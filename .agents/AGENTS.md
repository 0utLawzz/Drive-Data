# Global Workspace Rules

- **Always Push to GitHub**: At the end of every workflow, ensure that all changes are committed and pushed to the remote GitHub repository. Make sure to run `git add .`, `git commit -m "<descriptive message>"`, and `git push`.
